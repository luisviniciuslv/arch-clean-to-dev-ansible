backend = "glx";
vsync = true;
use-damage = true;
shadow = true;
shadow-radius = 12;
inactive-opacity = 0.85;
active-opacity = 1.0;

# Blur (Estilo vidro fosco)
blur-method = "dual_kawase";
blur-strength = 2; # Valores menores (2 ou 3) diminuem a intensidade do desfoque
